# Crank wallet — base58-encoded secret key (or use CRANK_KEYPAIR_PATH)
CRANK_KEYPAIR=
# Or: path to JSON keypair file
# CRANK_KEYPAIR_PATH=~/.config/solana/id.json

# Solana RPC
RPC_URL=https://api.devnet.solana.com

# Server port
PORT=8080
